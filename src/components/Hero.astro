---
// ヒーローセクションコンポーネント
---

<!-- モバイル専用レイアウト -->
<div class="block lg:hidden hero-section-mobile">
  <div class="relative z-20 container-custom py-8 sm:py-12 h-full flex items-center">
    <div class="text-center space-y-8">
      <div class="space-y-6">
        <h1 class="text-3xl sm:text-4xl font-bold leading-tight text-white">
          次世代広告"SNS"で、<br />
          貴社に影響力を。
        </h1>
        
        <div class="bg-white text-blue-900 px-4 sm:px-6 py-2 sm:py-3 rounded-lg inline-block font-bold text-base sm:text-lg">
          日本全国｜TikTok運用代行ならBuzzLiminal
        </div>
      </div>

      <!-- モバイル用スマホ画像 -->
      <div class="flex justify-center">
        <div class="w-48 h-72 bg-white rounded-3xl shadow-2xl p-4 transform rotate-12">
          <div class="w-full h-full bg-black rounded-2xl flex items-center justify-center">
            <div class="text-white text-4xl">📱</div>
          </div>
        </div>
      </div>

      <div class="flex justify-center">
        <button class="text-white font-bold text-lg sm:text-xl px-8 sm:px-12 py-3 sm:py-4 rounded-full transition-all duration-200 hover:scale-105 flex items-center space-x-3 cursor-pointer" style="background-color: #03C755;">
          <img src="/LINE_Brand_icon.png" alt="LINE" class="w-5 h-5 sm:w-6 sm:h-6" />
          <span>LINE相談はこちら</span>
        </button>
      </div>
    </div>
  </div>
</div>

<!-- デスクトップ専用レイアウト -->
<div class="hidden lg:block hero-section-desktop">
  <div class="relative z-20 container-custom h-full flex items-center">
    <div class="grid grid-cols-2 gap-12 items-center w-full px-8">
      <!-- 左側：テキストコンテンツ -->
      <div class="space-y-8 text-white ml-16">
        <div class="space-y-8">
          <p class="text-base leading-relaxed">
            SNSで新たな発見を。貴社のサービスや魅力を、届けるべき人に届けます。
          </p>
          
          <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold leading-tight">
            次世代広告"SNS"で、<br />
            貴社に影響力を。
          </h1>
          
          <div class="bg-white text-blue-900 px-6 py-3 rounded-lg inline-block font-bold text-lg">
            日本全国｜TikTok運用代行ならBuzzLiminal
          </div>
        </div>

        <div class="flex justify-start">
          <button class="text-white font-bold text-xl px-12 py-4 rounded-full transition-all duration-200 hover:scale-105 flex items-center space-x-3 cursor-pointer" style="background-color: #03C755;">
            <img src="/LINE_Brand_icon.png" alt="LINE" class="w-6 h-6" />
            <span>LINE相談はこちら</span>
          </button>
        </div>
      </div>

      <!-- 右側：スマホ画像 -->
      <div class="relative flex justify-center">
        <div class="relative">
          <!-- スマホの画像はここに実際の画像を配置 -->
          <div class="w-64 h-96 bg-white rounded-3xl shadow-2xl p-4 transform rotate-12">
            <div class="w-full h-full bg-black rounded-2xl flex items-center justify-center">
              <div class="text-white text-6xl">📱</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  /* モバイル専用スタイル */
  .hero-section-mobile {
    position: relative;
    background-image: url('/scramble.jpg');
    background-size: cover;
    background-position: center;
    overflow: hidden;
    margin-top: 0; /* ヘッダーの実際の高さ分のマージン */
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-section-mobile::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(59, 130, 246, 0.7); /* 青いオーバーレイ（濃く） */
    z-index: 1;
  }

  .hero-section-mobile > * {
    position: relative;
    z-index: 2;
  }

  /* デスクトップ専用スタイル */
  .hero-section-desktop {
    position: relative;
    background-image: url('/scramble.jpg');
    background-size: cover;
    background-position: center;
    overflow: hidden;
    margin-top: 128px;
    height: 570px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-section-desktop::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(59, 130, 246, 0.7);
    z-index: 1;
  }

  .hero-section-desktop > * {
    position: relative;
    z-index: 2;
  }

  /* 強制的な表示制御 */
  @media (min-width: 1024px) {
    .hero-section-mobile {
      display: none !important;
    }
  }

  @media (max-width: 1023px) {
    .hero-section-desktop {
      display: none !important;
    }
  }
</style>